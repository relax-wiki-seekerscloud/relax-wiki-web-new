import { GPMostPopularHotels } from './gp-most-popular-hotels';

describe('GPMostPopularHotels', () => {
  it('should create an instance', () => {
    expect(new GPMostPopularHotels()).toBeTruthy();
  });
});
